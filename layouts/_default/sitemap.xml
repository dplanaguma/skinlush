<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{ range .Data.Pages }}
    {{ if not (or (.Params.sitemapExclude true) (in .Site.Params.taxonomiesExcludedFromSitemap .Data.Plural)) }}
      <url>
        <loc>{{ .Permalink }}</loc>
        {{ if not .Lastmod.IsZero }}
          <lastmod>{{ .Lastmod.Format "2006-01-02T15:04:05-07:00" }}</lastmod>
        {{ else }}
          <lastmod>{{ now.Format "2006-01-02T15:04:05-07:00" }}</lastmod>
        {{ end }}
        <changefreq>daily</changefreq>
        <priority>{{ if ge .Sitemap.Priority 0.0 }}{{ .Sitemap.Priority }}{{ else }}0.8{{ end }}</priority>
        {{ if .IsTranslated }}
          {{ range .Translations }}
            <xhtml:link
                rel="alternate"
                hreflang="{{ .Language.Lang }}"
                href="{{ .Permalink }}"
            />
          {{ end }}
        {{ end }}
      </url>
    {{ end }}
  {{ end }}
</urlset>